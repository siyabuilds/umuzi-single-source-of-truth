// Shared input and result types used across repositories and the RAG pipeline.
// Note: SlabContent, QuestionAsked and GoogleDocsContent base types are generated by Prisma —
// import those directly from "../generated/prisma" instead.

import type { SlabContent, GoogleDocsContent } from "./generated/prisma";

// Input type for creating a new slab content record
export interface CreateSlabContent {
  title: string;
  chunk_text: string;
  embedding_vector?: number[];
  slab_url?: string;
}

// Input type for logging a new question
export interface CreateQuestionAsked {
  user_id: string;
  question_text: string;
}

// Vector search result with similarity score attached
export interface SlabContentWithSimilarity extends SlabContent {
  similarity: number;
}

// Input type for creating a new Google Docs content record
export interface CreateGoogleDocsContent {
  title: string;
  chunk_text: string;
  embedding_vector?: number[];
  doc_url?: string;
}

// Vector search result with similarity score attached for Google Docs
export interface GoogleDocsContentWithSimilarity extends GoogleDocsContent {
  similarity: number;
}